FastAPI -> Express.js
Uvicorn -> webserver
pydantic -> for type safety in python

steps:

1. python3 -m venv name-of-viertual-env
2. source venv/bin/activate -> activate the virtual env else system will use system's env
3. write fast apis
4. python -m uvicorn main:app --reload -> run the server
